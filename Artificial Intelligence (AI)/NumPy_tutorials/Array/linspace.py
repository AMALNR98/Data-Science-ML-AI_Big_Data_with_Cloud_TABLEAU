# linspace It’s somewhat similar to the NumPy arange function, in that it creates sequences of evenly spaced numbers
# structured as a NumPy array.
import numpy as np

matrix_1 = np.linspace(1, 30, 5 )
print(matrix_1)
